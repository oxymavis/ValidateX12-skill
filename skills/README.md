# Skills directory

Set **`{skills_dir}`** to this folder (`…/ValidateX12-skill/skills/`). Layout:

```text
skills/
  └── edi-validate-studio-declarative/
      ├── SKILL.md
      ├── manifest.json
      └── formats/
```

Each subdirectory of `skills/` is one skill and must contain **`SKILL.md`**.
